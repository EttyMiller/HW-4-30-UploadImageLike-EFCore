@using HW_4_30_EFCoreWeb.Data

@model ViewImageViewModel;

<div b-gx69rlccnc class="container">
    <main b-gx69rlccnc role="main" class="pb-3">

        <div class="row">
            <input type="hidden" id="image-id" value=@Model.Image.Id />
            <div class="col-md-10 offset-md-1 bg-light p-4 rounded shadow">
                <h1>@Model.Image.Title</h1>
                <img src="/images/@Model.Image.ImagePath" style="width: 900px;" />
                <br />
                <br />

                <button id="like-button" class="btn btn-primary btn-lg" disabled=@(Model.MyLiked != null && Model.MyLiked.Contains(Model.Image.Id) ? true : false)>LIKE</button>
                @*Alternate longer way   
                @if(Model.MyLiked != null && Model.MyLiked.Contains(Model.Image.Id))
                {
                    <button id="like-button" class="btn btn-primary btn-lg" disabled>LIKE</button>
                }
                else
                {
                <button id="like-button" class="btn btn-primary btn-lg" >LIKE</button>             
                }*@
                Current Likes: <span id="likes-count">@Model.Image.Likes</span>

            </div>
        </div>
    </main>
</div>

@section Scripts {
    <script src="~/js/ViewImage.js"></script>
}